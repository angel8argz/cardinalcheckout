# Copy to .env and adjust if your local Postgres differs.
# Matches the bundled docker-compose.yml (user/password/db).
DATABASE_URL="postgresql://cardinal:localdev@localhost:5432/cardinalcheckout?schema=public"
