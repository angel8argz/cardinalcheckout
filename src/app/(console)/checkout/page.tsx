import { Placeholder } from "@/components/Placeholder";

export default function CheckoutPage() {
  return (
    <Placeholder
      title="Checkout"
      blurb="Scan a patron card, then scan each resource going out. The keyboard-first checkout flow lands in a later session."
    />
  );
}
