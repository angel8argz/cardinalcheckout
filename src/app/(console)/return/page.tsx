import { Placeholder } from "@/components/Placeholder";

export default function ReturnPage() {
  return (
    <Placeholder
      title="Return"
      blurb="Scan each resource coming back and the system finds its open loan and checks timing. Coming in a later session."
    />
  );
}
